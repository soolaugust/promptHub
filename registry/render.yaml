services:
  - type: web
    name: prompthub-demo
    runtime: docker
    dockerfilePath: ./registry/Dockerfile
    plan: free
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: REGISTRY_CONFIG
        value: /etc/prompthub/registry.yaml
