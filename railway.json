{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "registry/Dockerfile"
  },
  "deploy": {
    "startCommand": "ph-registry /etc/prompthub/registry.yaml",
    "restartPolicyType": "ON_FAILURE"
  }
}
